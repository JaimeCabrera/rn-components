import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    // flex: 1,
  },
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  },
});
